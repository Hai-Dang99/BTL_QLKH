@model IEnumerable<QLKH.Models.nhacc>

@{
    ViewBag.Title = "Index";
}

<h2>Danh sách nhà cung cấp</h2>

<p>
    @Html.ActionLink("Thêm mới nhà cung cấp", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenncc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sdt)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenncc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sdt)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.mancc }) |
            @Html.ActionLink("Chia tiết", "Details", new { id=item.mancc }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.mancc })
        </td>
    </tr>
}

</table>
